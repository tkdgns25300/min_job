# MinJob — 기획서

> 페이지·기능·역할 명세. 데이터 모델은 [`DATA.md`](./DATA.md), 아키텍처·컨벤션은 [`../CLAUDE.md`](../CLAUDE.md), 작업은 [`ROADMAP.md`](./ROADMAP.md).
>
> 구체 필드·enum 허용값은 여기 적지 않는다(축·개념만). 값은 DATA.md에서 확정한다.

## 개요

- **프로젝트명**: MinJob (민잡) — ministry + job
- **한 줄 설명**: 흩어진 교회 사역자 청빙 공고를 모아 구조화·비교하게 해주는 채용 플랫폼
- **타겟 시장**: 한국 개신교(기독교) 교역자 청빙 전반 — 특정 교단에 한정하지 않는다
- **초기 집중 거점(beachhead)**: 예장합동·예장통합 (신학교 커뮤니티 접근성 + 개발자 인맥). **시장은 넓게, 초기 공고 확보·마케팅은 한 거점에 집중**
- **타겟 사용자**: 사역지를 구하는 교회 사역자(부목사·전도사 중심, 담임목사 포함), 청빙하는 교회
- **수익 모델**: 등록 무료 + 프리미엄 노출 유료 (교회 측 과금)

## 배경 (왜 이 제품인가 — 핵심만)

청빙 정보가 여러 신학교 게시판·교단 게시판·청빙넷 등에 흩어져 있어, 구직 교역자는 여러 사이트를 순회해야 한다. 기존 사이트들은 2000년대식 게시판이라 검색·필터·비교가 빈약하고, 공고가 자유 텍스트라 조건 비교가 안 된다. 청빙넷이 이미 "모아보기"를 표방하므로, MinJob의 차별점은 단순 수집이 아니라 **① 구조화(필터·정렬·비교) ② 신뢰 정보(재공고 추적) ③ 교회 정보 연결(홈페이지·유튜브 링크)**이다.

**스코프 — 교회 사역자 청빙을 다룬다. 주력은 부교역자(부목사·전도사).** 담임목사 청빙은 노회 승인·공동의회 결의·청빙위원회 등 무거운 공식 절차가 얽혀 초기 물량은 적을 것으로 보이나, 실수요가 있어 **직분 옵션으로 포함**한다 (타겟 인터뷰 2026-07-02, [`INTERVIEWS.md`](./INTERVIEWS.md)).

> ⚠️ 사용자 니즈는 구직 전도사 1명(개발자 누나) 인터뷰 기반 **가설**이다. 운영하며 검증·갱신한다.

## 사용자 모델 (3역할)

| 역할 | 설명 | 주요 행위 |
|---|---|---|
| **구직자(seeker)** | 사역지를 찾는 교회 사역자 | 공고 검색·필터·열람(로그인 불필요), 로그인 시 북마크, (Phase 3) 프로필 등록 |
| **교회(church)** | 청빙하는 교회 담당자 | 공고 등록·관리, 프리미엄 노출 결제 |
| **운영자(admin)** | 본인(개발자) | 수집 공고 AI 구조화·등록, 공고 관리 |

- 구직자는 **로그인 없이도 공고 열람 가능**(공개). 북마크·알림 등만 로그인 필요.
- 초기 공고는 운영자가 manual seeding으로 채운다('운영자 등록'). 교회는 가입 후 자기 공고를 직접 등록·관리한다(운영자 등록 공고와 병존).

## 핵심 기능 — 접근 영역별

> 표준 채용 사이트(잡코리아·원티드·사람인) 공통 요소 중 **1인 운영 + "지원은 교회 연락처로 직접" 모델에 맞는 가벼운 것만** 채택. 이력서 DB·사이트 내 지원·포지션 제안 같은 무거운 양면 거래는 Phase 3로 미룬다.

### A. 공개 영역 (로그인 불필요) — 트래픽 핵심

- **홈 (`/`)**: 검색 + 스탯(모집 중·새 공고) + 추천 청빙(AD) + 최신 공고 — 상세는 아래 "페이지별 섹션(확정)"
- **공고 목록 (`/jobs`)**: 검색·필터(교단·지역·직분·부서·고용형태·사례비)·정렬. 구직자 메인 화면
- **공고 상세 (`/jobs/[id]`)**: 구조화 정보 + 교회 홈페이지·유튜브 링크(★ 핵심 페인 해결) + 재공고 정보 + 지원 안내(원문 보기 / 교회 홈페이지 — 사이트 내 지원 없음) + JobPosting JSON-LD
- **교회 상세 (`/churches/[id]`)**: 교회 정보 + 홈페이지·유튜브 + 그 교회 공고 이력(재공고 패턴 가시화). 공고 상세에서 진입 — **교회 목록(browse)은 두지 않음**(구직자는 공고를 검색하지 교회를 훑지 않음; `/jobs` 교단·지역 필터가 대체, 리뷰·연봉 같은 enrichment도 없어 값 약함. 향후 필요 시 "교회 검색" 형태로만)
- **소개 (`/about`)**: 서비스 소개·신뢰 구축
- **공고 노출 안내 (`/pricing`)**: 노출 상품 안내(교회 대상). **top nav 아님** — footer + 교회 여정 맥락(공고 등록/관리·AD 섹션). (내용은 아래 "수익화" 섹션)
- **약관·개인정보처리방침 (`/terms`, `/privacy`)**: 로그인·개인정보를 다루므로 **필수**
- **문의**: 푸터 `mailto` (초기엔 폼 X — 스팸·운영 부담)

### B. 인증 영역 (로그인 필요)

- **로그인 (`/login`)**: 간편 로그인(카카오 우선) + 이메일 fallback. 가입 마찰 최소화
- **공고 등록·수정 (`/jobs/new`, `/jobs/[id]/edit`)**: 교회가 직접 올리는 폼. 등록 **무료**. '교회 직접 등록', 본인이 소유자
- **마이페이지 (`/mypage`)**:
  - 교회: 내가 등록한 공고 관리
  - 구직자: **북마크**(스크랩한 공고) — 표준 기능, 가볍고 재방문 유도 (Phase 2)
- **(선택) 관심 조건 알림** (Phase 3): 표준 기능이나 알림 발송·운영 부담이 커 후순위

### C. 운영자 영역 (admin — 최소 형태로 유지)

거창한 시스템 ❌, **공고 입력 작업대**로만:

- **공고 등록 도구 (`/admin/ingest`)**: 수집한 공고 텍스트 붙여넣기 → AI가 교회·공고 필드 자동 구조화 → 검토 후 등록('운영자 등록', 소유자 없음). 운영자 핵심 작업 화면. ★ 자동 크롤러 아님(가드레일 #1)
- **공고 관리 (`/admin/jobs`)**: 등록 공고 수정·삭제·상태 변경
- → 대시보드·통계·교회관리 등 별도 admin 시스템은 **만들지 않는다**

## 수익화 — 광고·노출 (첫 매출 채널)

등록은 무료, **노출이 유료**. 교회가 결제하면 공고를 "더 많이·더 크게·눈에 띄는 위치"에 보여준다.

- **노출 등급**: 일반 / 프리미엄(목록 상단 고정 + 강조 배지) / 대표 광고(홈·목록 상단 추천 슬롯에 더 크게)
- **노출 위치·크기·기간 차등**: 요금에 따라 노출 위치·카드 크기·노출 기간을 다르게. 기간 만료 시 자동 강등
- **정렬 반영**: 프리미엄·대표 광고가 정렬에서 우선(정렬·필터 규칙 표 참조)
- 결제는 초기엔 수동 처리, 자동 결제 연동은 Phase 3

> **노출 상품은 2종(프리미엄·대표광고)으로 진행**한다. 끌어올리기(bump)는 공고량이 적어(오래된 공고도 안 묻힘) 값이 약해 **제외**(공고량 늘면 재검토). 각 상품의 **상세(노출 위치·기간 단위·가격·묶음 할인·부가세·결제 수단)와 최종 확정은 Phase 2에서 다시 검토**한다. 그전까지 `/pricing`은 "안내 + 문의" 수준(라이브 결제·구좌 예약 없음). *사용자 노출 라벨 = **"공고 노출 안내"**.* 배너 광고(기독 B2B)는 별도 광고주·ad ops 필요 → Phase 2+ 옵션(레일 슬롯만).

## 의도적으로 안 하는 것 (스코프 보호)

- **사이트 내 지원·이력서 DB·포지션 제안** → Phase 3. 무거운 양면 거래라 1인 운영 제약과 충돌. 초기엔 "지원은 교회 연락처로 직접".
- **교회 리뷰·평점** → 하지 않음. 좁은 기독교 공동체에서 명예훼손·분쟁 리스크가 큼. "신뢰 정보"는 재공고 추적 같은 **객관 데이터로만** 제공.
- **거대 admin 대시보드·통계** → 안 함(1인 운영).

## 정렬·필터 규칙 (전역 일관)

> 코드·UI 모두 이 표 기준. 구체 enum 값·SQL은 DATA.md.

| 위치 | 정렬 |
|---|---|
| 공고 목록(기본) | 대표광고·프리미엄 먼저 → 게시일 ↓(최신) |
| 공고 목록(사례비순) | 대표광고·프리미엄 먼저 → 사례비 상한 ↓ |
| 공고 목록(마감임박) | 대표광고·프리미엄 먼저 → 마감일 ↑(가까운 순). 마감 없는 공고는 뒤로 |
| 교회 상세 — 공고 | 게시일 ↓ |

| 필터 축 | 비고 |
|---|---|
| 교단 | 허용값 DATA.md (개신교 전 교단 확장 가능) |
| 지역 | 허용값 DATA.md (광역 단위) |
| 직분 | 허용값 DATA.md |
| 부서 | 허용값 DATA.md |
| 고용형태 | 허용값 DATA.md |
| 교회 규모 | 개척·소형·중형·대형 + 미상 (`churches.size`) |
| 사례비 | 범위(min~max) + "협의 포함" 토글 |

## 비즈니스 규칙

- **등록 무료, 노출 유료**: 공고 등록에 과금하지 않는다. 프리미엄·대표광고 노출만 유료.
- **과금 주체는 교회**: 구직자는 전부 무료(사람을 모으기 위함).
- **공고 출처 표기**: 운영자가 등록한 공고는 "원문 보기" 링크를 제공한다. 교회가 직접 등록한 공고는 그 교회가 관리한다.

## 비기능 요구

- **검색 응답**: 필터·검색 결과가 빠르게(체감 지연 최소). 공고 목록은 캐시 서빙.
- **데이터 정직성**: 사례비 등 비정형 표현은 왜곡 없이 원문 보존(필드 설계는 DATA.md). 공고 본문 원문 통째 복제 금지(요약·구조화).
- **개인정보**: 로그인·간편 로그인·북마크·알림으로 개인정보를 처리하는 순간 **약관·개인정보처리방침·수집이용 동의가 법적으로 필수**(로그인 기능과 동시에 갖춘다). 정책은 DATA.md.
- (UI 규칙·모바일 퍼스트는 CLAUDE.md Styling.)

## 우선순위 (개요 — 작업 단위는 ROADMAP.md)

- **Phase 1 (MVP)**: 공개 열람(홈·목록·상세·교회) + 검색·필터, 교회 공고 등록·수정, admin(ingest+jobs), 로그인(간편), 소개·약관·개인정보·문의
- **Phase 2 (차별화)**: 재공고 추적, 거리 기반 필터, 광고·노출(프리미엄·대표광고), 북마크(+구직자 로그인)
- **Phase 3 (고도화)**: 인재 DB, 사이트 내 지원 중개, 결제 연동(자동 과금), 공고 재등록 편의, 관심 조건 알림, 검색광고

## 디자인 방향

- 깔끔하고 신뢰감 있는 톤. 기존 청빙 사이트의 구식 게시판과 대비되는 **현대적 UI**가 차별화의 일부.
- 공고 카드가 한눈에 비교되도록 정보 위계를 명확히 — 작은 화면에서도 핵심 조건이 바로 읽히게.
- 구조화된 정보(교단·지역·사례비·부서)를 배지·라벨로 시각화해 "비교 가능함"을 체감하게.

## 페이지별 섹션 (확정 페이지)

> 사용자가 최종 확인한 페이지의 섹션 구성 기록. 페이지를 완성할 때마다 추가한다(유지보수용).

### 홈 `/` — 재설계 2026-07-06 (딥그린 브랜드)
1. **헤더**(전역·딥그린) — 로고(골드 Min) + 공고 + 로그인. 교회 등록 CTA는 사이드바로(헤더 제거)
2. **히어로**(딥그린 풀블리드·중앙) — eyebrow + 헤드라인 "다음 사역지, 여기서 찾으세요" + **검색 오버레이**(최근검색어·최근본공고·검색어완성) + 지표 3개(지금 모집 중 · 이번 주 새 공고 · 함께하는 교회)
3. **추천 청빙 (대표광고)** — HERO 전용 슬롯, `FeaturedJobCard`(초록 테두리 카드, 2열)로 도드라지게
4. **2단** — 좌 **청빙 공고 리스트**(`JobRow`: 제목 주인공·텍스트만·프리미엄=태그+상단고정·일반) + "전체 공고 보기"(→`/jobs`) / 우 **사이드바**(추천 검색어 칩→`/jobs?q=` · 교회 CTA→`/jobs/new`)
5. **3-피처**(하단, 예정) — 한 곳에 모아보기 · 구조화된 비교 · 재공고 추적
- **노출 등급 차등**: 대표광고=별도 카드 슬롯 / 프리미엄=리스트 상단 고정+태그 / 일반=리스트 (틴트 없이 organic에 가깝게 — 표시광고법·신뢰)
- **제외**: 개인화 추천 · 온보딩 · 인기 랭킹 · 카테고리 탐색(→`/jobs`) · 툴 · 콘텐츠 · **고용형태 탭(뺌)**
- **근거**: 저빈도·1인 운영·검색 의도·종교 민감성

### 공고 목록 `/jobs` — 확정 2026-07-01
1. **검색** — 교회명·지역·직분 (결과 내)
2. **대표 광고 (AD) 섹션** — 상단 고정, HERO 공고 (항상 노출·프라임)
3. **좌 필터 사이드바** — 교단·지역·직분·부서·고용형태·**교회 규모**(칩 토글, 8개↑ 그룹은 "더보기") + 월 사례비 range + "협의 포함"
4. **목록 헤더** — 총 N건 + 정렬(최신·사례비·마감임박)
5. **공고 리스트** — 프리미엄 상단 고정 → 일반 (대표광고는 제외 → 2번 AD 섹션에만)
6. **우측 레일**(데스크톱) — 최근 본 공고(localStorage) + 배너 광고 슬롯(B2B)
7. **페이지네이션** — 8건/페이지 (페이지 번호)
8. **모바일** — 필터 Sheet(드로어), 우측 레일 숨김
- **광고 배치(중복 0)**: 대표광고=AD 섹션 / 프리미엄=목록 상단 고정 / 배너=레일
- **제외**: 기업형태 탭 · 즉시지원/확인한공고 체크 · 이미지 카드 · 무한스크롤 · 미리보기 · 개인화 추천 (이유는 대화 기록)
- **mock 단계**: 클라이언트 필터. 실제 데이터 연동 시 서버 렌더(목록 cache + 필터 dynamic `?page=` URL)로 전환

### 공고 상세 `/jobs/[id]` — 확정 2026-07-01
**레이아웃**: 2단(데스크톱) — 좌측 본문 + 우측 sticky 요약 사이드바. 모바일은 한 컬럼, 사이드바가 위로. **본문은 카드 남발 없이 한 면에서 제목+구분선으로 흐름**(파편화 방지).

좌측 본문(카드 1개):
1. **헤더** — 교회 아바타 + 교회명(→교회상세)·교단·지역·규모 · 등록일 · 저장·공유(client) / **재공고 배지 + 직전 마감일** / 제목 / 직분·부서·고용형태
2. **자격 요건** (체크 리스트)
3. **우대 사항** (체크 리스트)
4. **공고 안내** (본문)
5. **교회 정보** — 교회 요약 + 교회 상세 링크 + 이 교회의 다른 모집

우측 사이드바(sticky):
- **핵심 조건** — 월 사례비(강조)·마감일·출근·고용형태·제출서류
- **지원 안내** — CTA "원문 공고 보기"(`sourceUrl`) or "교회 홈페이지"; *사이트 내 지원 없음*(교회 직접 문의 안내)
- ★ **교회 채널** — 홈페이지·유튜브·SNS 링크(`church.links`, 공유 컴포넌트 `ChurchChannels`) (핵심 페인, 스크롤 내내 고정)

2단 하단: **비슷한 공고**(같은 부서→같은 지역, JobCard 그리드) · **출처 표기**(운영자/교회) + 오류 문의

- **제외**: 지원자통계·AI합격률 / 사이트 내 즉시지원 / 이미지 갤러리·**포스터 이미지**(재호스팅 저작권 위험) / 복리후생 카드 / 자소서 항목 / 회사특성 태그 / 담당자 개인 연락처(가드레일) / 근무지 지도(Phase 2)
- **SEO**: `generateMetadata`(title·desc·OG) + `JobPosting` JSON-LD(`lib/seo.ts`)
- **캐싱**: params 의존 → 동적 콘텐츠를 `<Suspense>`로 분리(정적 셸 + PPR). 추후 on-demand `'use cache'`(Phase 1)
- **이 페이지가 확정한 `jobs` 필드**: `workDays · requirements[] · preferred[] · requiredDocs[] · description · source(OPERATOR/CHURCH) · sourceUrl`. (포스터 이미지 필드는 미채택)

### 교회 상세 `/churches/[id]` — 확정 2026-07-01
**레이아웃**: 단일 컬럼(neutral). 결정 페이지가 아니라 허브라 사이드바 없음. 공고 상세의 "교회 상세 보기"에서 진입.
1. **헤더** — 이니셜 아바타 + 교회명 + 교단·지역·규모·**창립연도**(`foundedYear`, 미상 시 생략) + **교회 채널**(`ChurchChannels` 공유 컴포넌트, 홈페이지·유튜브·인스타·페북·밴드)
2. **현재 모집 N건** — 그 교회 OPEN 공고(JobCard 그리드, 빈 상태 처리)
3. ★★ **공고 이력**(차별점, 시안 C) — 재공고 시 **요약 문장**("이 교회는 OO 자리를 최근 N번 공고했어요") 항상 노출 + **`<details>` 토글**(JS 없이)로 자리별 타임라인 상세(각 공고 `등록~마감`·`모집중/마감`·공고 상세 링크). `lib/repost-tracking`의 `groupByRole`.
   - **현재 단발 공고는 이력에서 제외**(위 카드에 이미 있음), **반복·지난 공고가 없으면 섹션 숨김**(현재 공고 반복으로 비어 보이는 것 방지).

- **제외**: 교회 리뷰(현직자 평가 — 명예훼손·데이터 없음 → 재공고 이력=사실 기반으로 대체) · 연봉/급여·재무·인원 통계(데이터 소스 없음) · 복리후생/BEST문화 배지 · 팔로우·채용알림(Phase 2+) · 커버/로고 이미지 · 유사 추천 교회
- **정체성**: 의도적으로 얇음 — 순수 신규 가치 = **재공고 타임라인 + SEO 교회 허브**. 리뷰·연봉을 조작해 채우지 않는다(integrity·가드레일).
- **채널 스키마**: `church.links: {type: ChurchChannel, url}[]` — 채널 추가는 `CHURCH_CHANNELS` enum에만(마이그레이션 X)
- **SEO**: `generateMetadata`(title·desc·OG) · **캐싱**: params → `<Suspense>`(PPR)

### 공고 노출 안내 `/pricing` — 확정 2026-07-02
**정적 페이지**(교회=파는 쪽 대상). **top nav 아님** — footer "공고 노출 안내" + 헤더 "교회 공고 등록" CTA + (옵션)AD 섹션 맥락으로 진입. 가격은 전부 **"문의"**(실확정·라이브 결제는 Phase 2).
1. **헤더(가치 제안)** — "청빙 공고를 더 많은 교역자에게" + **등록은 무료** 강조(거부감 완화)
2. **노출 상품** — 프리미엄 / 대표광고 2종 카드(태그라인 · 노출 위치 · `가격 문의`)
3. **노출 위치 미리보기** — 공고 목록 와이어프레임 목업(대표광고→프리미엄→일반) + 상품별 설명. *우리가 정직하게 보여줄 수 있는 핵심 설득 자료*
4. **이용 방법** — 3스텝(공고 등록[무료] → 노출 상품 선택 → 문의로 진행)
5. **FAQ** — `<details>` 아코디언(등록 무료·노출 시점·결제·세금계산서)
6. **CTA** — "교회 공고 등록"(→`/jobs/new`, 로그인 게이트) + "노출 문의"(mailto)

- **제외**: 라이브 결제·구좌 예약 캘린더·상품별 세부 페이지(대형 플랫폼식) · 배너/텍스트 광고 · 끌어올리기 — 상품 2종·미확정 가격엔 과함. 서브페이지 없이 단일 페이지
- **SEO**: 정적 `metadata` · 정적 prerender(`○`)

### 소개 `/about` — 확정 2026-07-02
**정적 페이지**. **top nav 아님 — footer 전용**(현업 표준: 회사소개는 footer). 톤은 **담백·정직**(과장·판매성 X). 신규 니치 서비스라 "무엇/왜"를 이해시키는 게 목적.
1. **소개** — 무엇인지 한 문단(흩어진 청빙 공고를 한 곳에 모아 지역·교단·직분·부서로 찾고 비교)
2. **왜 만들었나요** — 배경(신학교·교단 게시판 등에 흩어져 한눈에 어려움 → 모아 정리)
3. **무엇을 할 수 있나요** — 검색·필터 / 정리된 정보 비교 / 교회 채널 이동 / 재공고 이력 (담백 나열)
4. **이런 분들께** — 청빙 찾는 교역자 · 사역자 찾는 교회
5. **문의** — mailto

- **제외**: 데이터 수집 방식·신뢰 섹션(밝히지 않음) · 히어로 배너 · 차별점 자랑 · 판매성 CTA · 연혁·팀·IR(1인·신규라 과함)

### 헤더/네비 (구직자 중심 + 파는쪽 분리)
- **좌 nav = `공고` 하나**(공개 탐색 기능이 공고뿐 — 콘텐츠·커뮤니티·툴 등 미보유). 로고=홈.
- **우측 = 로그인 + 교회 공고 등록**(파는쪽 CTA = 현업 "기업서비스" 대응). 인증 후(Phase 1) 마이페이지·내 공고 관리는 **우측 계정(아바타) 영역**으로 — 좌 nav는 계속 얇게.
- **footer** = 소개 · 공고 노출 안내 · 이용약관 · 개인정보처리방침 · 문의

### 이용약관 `/terms` · 개인정보처리방침 `/privacy` — 초안 2026-07-02
**정적 페이지 · footer 전용**. ⚠️ **현재 내용은 초안(placeholder)** — 페이지 상단에 "정식 운영 전 법률 검토" 고지 + 시행일 미정. **정식 운영 전 반드시 법률 검토 후 확정**(ROADMAP 1-6).
- **이용약관**: 목적·정의·효력/변경·회원가입·서비스 내용·이용자 의무(자동수집 금지 등)·공고 책임(교회/운영자 구분)·이용제한/해지·면책·지식재산권·준거법·부칙
- **개인정보처리방침**: 수집 항목·이용 목적·보유기간·제3자 제공·처리 위탁·정보주체 권리·파기·안전성 조치·쿠키·보호책임자·변경 고지
- 표준 조항 골격은 `SECTIONS` 배열로 관리(제목+본문/리스트 map 렌더)