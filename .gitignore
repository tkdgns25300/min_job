# dependencies
/node_modules

# next.js
/.next/
/out/

# production build
/build

# env
.env
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# misc
.DS_Store
*.log

# claude code (local)
.claude/settings.local.json

# Supabase CLI 로컬 캐시 (버전 확인용 — 커밋 대상 아님)
supabase/.temp/

# MCP 서버 설정 — 개발 도구용이고 앱 동작과 무관하다. 리포가 공개라 프로젝트 ref를
# 굳이 노출하지 않는다(시크릿은 없지만 브라우저 번들에도 없는 값이다).
.mcp.json
